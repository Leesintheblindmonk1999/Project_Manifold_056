# ==============================================================================
# Project Manifold 0.56 — The Invariance Engine
# Requirements File
# Author: Gonzalo Emir Durante
# License: Durante Invariance License v1.0
# Registration: EX-2026-18792778- -APN-DGDYD#JGM
# ==============================================================================

# ------------------------------------------------------------------------------
# Phase I & II — Core Geometry (constants.py, crystal_engine.py)
# ------------------------------------------------------------------------------
numpy>=1.24.0               # Vectorised array operations, linalg, broadcasting
scipy>=1.11.0               # Scientific computing base (used by Ripser/Gudhi)

# ------------------------------------------------------------------------------
# Phase III — Persistent Homology (filtration.py, persistence.py)
# ------------------------------------------------------------------------------
ripser>=0.6.8               # Vietoris-Rips persistent homology (Ripser++ backend)
persim>=0.3.3               # Persistence diagram distances & plot_diagrams()
gudhi>=3.8.0                # Gudhi TDA library — Alpha/Witness complexes, Simplex Tree

# ------------------------------------------------------------------------------
# Phase IV — Ricci Flow (ricci_flow.py)
# ------------------------------------------------------------------------------
# Core operations use NumPy only — no additional dependency.
# Optional: GraphRicciCurvature for discrete Ollivier-Ricci on graphs
GraphRicciCurvature>=0.5.3  # Discrete Ricci curvature on NetworkX graphs (optional)
networkx>=3.1               # Graph structures required by GraphRicciCurvature

# ------------------------------------------------------------------------------
# Phase V — Simplex Engine (simplex_engine.py)
# ------------------------------------------------------------------------------
# Core operations use NumPy only — no additional dependency.

# ------------------------------------------------------------------------------
# Phase VI — Safety & Certification (monitor.py, certification.py)
# ------------------------------------------------------------------------------
# Standard library only (uuid, hashlib, datetime, json) — no extra dependency.

# ------------------------------------------------------------------------------
# Utils — Visualisation & Helpers (plotting.py, helpers.py)
# ------------------------------------------------------------------------------
matplotlib>=3.7.0           # Barcodes, stability gauge, radar chart, full report
seaborn>=0.12.0             # Optional: enhanced styling for diagnostic plots

# ------------------------------------------------------------------------------
# Orchestrator & CLI (main.py)
# ------------------------------------------------------------------------------
# Standard library only (argparse, logging, time, pathlib) — no extra dependency.

# ------------------------------------------------------------------------------
# Data handling (data/ — point cloud loading & tensor conversion)
# ------------------------------------------------------------------------------
pandas>=2.0.0               # Tabular data loading for raw hallucination logs
scikit-learn>=1.3.0         # PCA, StandardScaler, pairwise_distances utilities

# ------------------------------------------------------------------------------
# Development & Testing
# ------------------------------------------------------------------------------
pytest>=7.4.0               # Test runner for tests/
pytest-cov>=4.1.0           # Coverage reports